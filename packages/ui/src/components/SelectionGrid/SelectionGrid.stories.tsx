import preview from '#storybook/preview';

import { SelectionGrid } from './SelectionGrid';

const meta = preview.meta({ component: SelectionGrid });

export const Primary = meta.story({
  args: {},
});
